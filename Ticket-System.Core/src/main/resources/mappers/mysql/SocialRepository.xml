<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apps.mybatis.mysql.SocialRepository">

    <insert id="insertGoogleAccount" >
        insert into google_account(user_info_id,google_id)
        values (#{userInfoId},#{googleId})
    </insert>

</mapper>